{
  "name": "sitelemetry-browser-extension",
  "version": "0.1.1",
  "private": true,
  "description": "Sitelemetry Audit: Manifest V3 extension for Chrome and Edge that runs a Sitelemetry security audit of the site in the current tab.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "check": "node scripts/validate-manifest.mjs && node scripts/check-no-remote-code.mjs",
    "icons": "node scripts/make-icons.mjs",
    "pack": "powershell -ExecutionPolicy Bypass -File scripts/pack.ps1"
  }
}
